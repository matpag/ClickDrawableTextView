package com.matpag.clickdrawabletextview;

/**
 * Created by Mattia Pagini on 13/02/2017.
 */

public enum DrawablePosition {
    START, //LEFT if not in RTL mode
    TOP,
    END,   //RIGHT if not in RTL mode
    BOTTOM
}